{
  "name": "andy-sd-godtier-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "dev:flipfindr": "npm run dev --workspace=apps/flip-findr-v2",
    "dev:gigdesk": "npm run dev --workspace=apps/gig-desk-mainframe"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0"
  }
}
